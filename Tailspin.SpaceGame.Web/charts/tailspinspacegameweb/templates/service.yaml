apiVersion: v1
kind: Service
metadata:
  name: {{ template "tailspinspacegameweb.fullname" . }}
  labels:
    app: {{ template "tailspinspacegameweb.name" . }}
    chart: {{ template "tailspinspacegameweb.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "tailspinspacegameweb.name" . }}
    release: {{ .Release.Name }}
